import { ReactElement } from "react";

import { Container, Title } from "./styles";

export const Groups = (): ReactElement => {
  return (
    <Container>
      <Title>Groups</Title>
    </Container>
  );
};
